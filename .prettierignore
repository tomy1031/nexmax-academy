# 生成物・依存
.next/
out/
build/
.open-next/
.wrangler/
cloudflare-env.d.ts
node_modules/
package-lock.json
.claude/
.tmp*

# ドキュメント（手組みの表・整形を保持したいため Prettier 対象外）
docs/
# 説明資料のHTML。人とAIが読む資料でコードではないので整形しない
# （docs/ 配下は上で除外済み。これはリポジトリ直下に置かれたぶん）
*.html

# コンテンツJSON（lint:content が別途スキーマ検証する）
content/

# ポップアップ辞書（content/vocab から 焼いた 1枚。ブラウザが 取りに 来る）。
# 整形すると 焼いた 中身と 1バイト ずれ、lint:content の ずれ検査が 落ちる。
public/dictionary/learner.json

# pdf.js の worker（npm install のたびに写す他人のコード。整形しない）
public/pdfjs/

# 旧アプリから そのまま 写した 練習ページの 共有部品（リンク教材の 行き先）。
# 整形すると 旧アプリとの 差分が 読めなくなり、写し直しの たびに 全行が 動く。
# 中身は 旧アプリが 正（scripts/migrate_hourensou_tools.mjs が 写す）。
public/tools/hourensou/app.js
public/tools/hourensou/styles.css
