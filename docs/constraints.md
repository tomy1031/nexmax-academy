# 制約台帳（constraints）

ユーザーが口頭で伝えた**製品・運用上の制約と好み**の永続台帳。
どのスレッド・セッションも**作業前にこのファイルを読む**（AGENTS.md 多スレッド運用ルール参照）。

**追記のルール**: ユーザーが会話で制約・好みを口にしたら、その場でここに1行追加して
コミットに含める（「前に言ったのに別スレッドに伝わっていない」を無くすため）。
日付と一言の背景を添える。既存項目と矛盾したら、古い方を打ち消し線で残して更新する。

## 製品の制約

| 制約 | 背景・出典 |
| ------------------------------------------------------------------------------ | ---------------------------------------------------- |
| 学習者向けの語彙・文言は N4 前後。「読めるか」が最優先 | 対象はカンボジアIT専攻学生。詳細は docs/design/01 |
| インフラ・API は**無料枠内**で運用する。有料化はユーザーの事前承認が必要 | 繰り返し明言（2026-07 会話） |
| ~~経由国の表記は「北回り／南回り」に寄せ、特定の国名を出さない~~ → **国名を出さないのは まなびマップ の中だけ**（`src/content/areas.ts`。エリアは景色の名前で呼び、経由国は北回り／南回りに寄せる）。**禁じているのは「タイ」だけ**で、それ以外の国名は**教材の本文に自由に書いてよい**（事前確認は要らない） | 2026-07 の指定は地図の見せかたの話だった。2026-08-23「タイが敵対関係にあるから出さないというだけ。国の名前の箇所は非表示でいいという指示を曲解されて今に至ります。mapで非表示にしてほしいだけ。タイ禁止以外のルールは無くしてほしい」。検査から `AGREED_PLACE_NAMES` / `CONFIRM_PLACE_NAMES`（要確認の warn）を削除（`src/lib/content-checks.ts`） |
| 完成している既存UIの**作り直しをしない**。改良のみ（全画面のゲーム感・臨場感を保つ） | 2026-07-29 単語ゲームUI事故の再発防止 |
| 学習者向け文言の禁止語（「不正解」「間違いです」「ダメ」） | AGENTS.md 絶対規律1（機械検査あり） |
| **生成は文章も画像も Codex（ChatGPTの枠）を第一にする。Gemini は控えに回す** | 2026-08-07「Gemini無料枠は少ない」。Codex は ChatGPT ログインだけで動くことを実測ずみ（docs/codex-backend.md §1） |
| **AI生成は必ず段階を踏む**（作りたいもの→AIが案→人が修正・承認→次の段階→承認→生成）。一発生成にしない | 2026-08-07「AIを使うためのフローが明確になるように」。漫画だけでなくリスニング等すべての教材で |
| AIは**過去に作った教材を踏まえて**新しい案を作る（前の課の語彙・登場人物・話の続き） | 2026-08-07 の指定 |
| まんがは**一枚絵**と**セリフ入り画像**の両方を作れるようにする | 2026-08-07 の指定 |
| 絵の吹き出しの文字は**かな限定にしない**。**漢字＋ふりがな・カタカナ・ローマ字（Zoom 等）も焼けるように**する。いただいたセリフを言い換えない | 2026-08-19「漢字+ふりがなやカタカナも対応可が望ましい」。当時の実装は `schema.ts` の `checkBakedText` がかな・20字に縛っていた（根拠だった「ふりがなは焼けない」は image-gen-2 で成功して覆った）。見直しは 願い #115 |
| **ログインが最初の画面**。ログインするまで中の画面を開けない。ログイン画面は別に置かず**タイトル画面が兼ねる**（ログアウトの戻り先もタイトル画面） | 2026-08-11 の指定（願い #13）。旧 `/login` は削除しタイトルへ送る |
| 学習者の**なまえはカタカナ**で持つ。「苗字・名前・先生に呼んでほしい名前」の3つに分け、カタカナ以外はやさしい言い方で書き直しを案内する | 2026-08-11 の指定（願い #14）。先生が読み方に迷わず呼べるように。判定は src/lib/name.ts に一本化 |
| Google 登録名の初期値は**カタカナのときだけ**欄に入れる。ローマ字のときは欄を空にして見本として見せる | 2026-08-11「空にして見本はOK」。カンボジアの学習者はほぼローマ字なので、開いた瞬間に注意が出ないようにする |
| なまえの**見本はカンボジアの名前**にする（ソク／ソピア／ピア など）。日本人名を見本にしない | 2026-08-11「カンボジア人っぽい名前でないと」 |
| 難しい語を**ひらがなに開かない**（ひらがなでも意味は分からない）。**漢字＋ふりがな**を保ち、N5を超える語は**英語で意味**を添える。ツールチップの説明文にもふりがなを付ける | 2026-07-28・08-04・08-05・08-11 に繰り返し明言。「ひらがなだけでわかりやすくなることは、決してありません」 |
| **30人同時アクセスに耐える**（Cloudflare の上限エラーを出さない）。全員が必ず通る画面で外部往復・重い処理を増やさない。学習者画面の Link は **prefetch={false}** を付け、画像は静的アセット直配信（`images.unoptimized`）、ISR の revalidate は **300秒**より短くしない。**学習者が通る画面を `dynamic`（`next build` の表の `ƒ`）にしない** —— dynamic はリクエストのたび Next のサーバ本体を読み込み、無料枠の CPU 上限（1リクエスト 10ms）を構造的に超える | 2026-08-11 STG で Error 1102 が実発生（タイトル画面の外部往復 afd932c で修正）。2026-08-25 単語ゲームの**20人同時プレイで上限超過が再発**し、先読み・画像・ISR書き込みを削減（docs/deploy.md §0.7） |
| 登場人物の 絵は **本人の 写真に 寄せる**。ニャムは 男性で ベトナムの 顔立ち（服ではなく **顔**で 国際色を 出す）。松井社長は 体は 普通で **顔だけ 気持ち** ふくよか。藤木取締役は 20代後半（ほうれい線を 描かない） | 2026-08-14 の指定。「気持ち」は ほんの少しの意。参考写真は `public/img/characters/<id>/reference.*` |
| 質問・教材の導入は、まず**「それが何か」の説明**から始める（「性格」とは何か等）。語の説明なしで質問を始めない | 2026-08-04「そもそも性格とか言葉の意味がわからない状態からのスタート」 |
| スライド教材は**PDFを自前で表示する**。外部ビューア（教材のURLを外の会社に渡して埋め込む方式）は使わない | 2026-08-14「私は1以外で考えたい。PDFをスライドにするとか」。パワポは先生が PDF で書き出して上げる |
| 企業調査教材の核心は**「他の会社と違う、その会社の面白い所」を見つける**こと。進行は「クローズド（穴埋め）→ ヘンディさんに報告して整理・整える → 松井社長と好感度を上げる対話（恋愛ゲーム風）」の3幕。数少ない日本語表現＋できる限りビジュアルで | 2026-08-14 の指定（願い #43・提案書 docs/proposals/2026-08-14） |
| **ロゴは どこでも 絵を 使う**。CSS の文字（グラデーション＋白フチ）で ロゴを 組まない。控え（絵が出せないとき）も 置かない | 2026-08-17「全てロゴは画像にしてください」。出しかたは `src/components/academy-logo.tsx` に一本化（幅だけを変える） |
| **大きな数は「万」を使わず 桁区切りで書く**（1.5万人 → 15,000人 / 79万人 → 790,000人） | 2026-08-17「1.5万人はわからないので、15,000人と書く。全部」。学習者に量が伝わらないため |
| **語彙の意味は スライドの中に置かず、ノート欄（💡）に「【日本語】English」で**書く | 2026-08-17 の指定。スライドの中の辞書チップは消す。ノート欄はルビ・語彙ポップアップが効く唯一の場所（`slide-deck.tsx`） |
| 辞書は**多いほど良いのではない**。①絵で想像できるもの ②基礎的な単語 ③カタカナ語だけど簡単にフォローできるもの ④一度紹介した語 は**載せない**（語彙の無いページは ノートを置かない） | 2026-08-18「消していい単語の例…はいらない」。要らない語が並ぶと覚えるべき語が埋もれる。2026-08-17 の「多めでよい」を上書き。検査は `scripts/slides/check_glossary.mjs`（取りこぼし・重複・幽霊の3つを見る） |
| スライドで**話が変わるところには章扉（タイトルスライド）**を入れる。前触れなく話題を変えない | 2026-08-17「今までのチーム。唐突。話が変わる箇所はタイトルスライドを間に入れたい」 |
| **数を宣言しない**（「大事なことは3つ」のような言い方をしない）。あとから増えるため | 2026-08-17「色々あるので3つとは言わないで」 |
| 出どころが古い数字は、**いつの数字かを必ず書く**（新しい数字があれば差し替える） | 2026-08-17「Microsoftの数が少ないのは情報が古いせい。新しい情報がないのならせめていつかを書いた方がいい」 |
| **カードの ふちを 白フチだけで 済ませない**。外側に 細い枠を 1本 置いて、上下左右の どこまでが 要素かを 見せる（下だけの 硬い影は 残してよい） | 2026-08-18「影の付け方が変。上下の縁や横の縁がないからどこまでが要素かわからない」。白い地に 白フチだと 境目が 消える |
| **色の面（濃い地に 白文字）の ふりがなは 白にする**。既定の `rt` は 濃い灰なので 紺・緑の チップの 上では ふりがなだけ 沈む | 2026-08-18「ボタンのカラーの場合のふりがなは白にして」。共有クラスは `RUBY_ON_COLOR`（src/components/ruby-text.tsx） |
| **むずかしい語は 漢字で 書き、語彙メモ（ツールチップ）で 意味を 出す**。やさしい日本語の 本文でも ひらがなに 開かない | 2026-08-18「単語の日本語表示をツールチップのような形で」。診断の 20問は 13問が 語彙メモ ゼロだった（開いた かな は 引き当てられない） |
| **診断の 語彙メモは「へや」「アプリ」「アイデア」くらいの 語まで 付ける**（カタカナ語も 入れる） | 2026-08-18「へや／アプリとか／アイデア などのレベルも」。**スライドのノート（上の「辞書は多いほど良いのではない」）とは 別方針**——スライドは 一覧に 並ぶので 要る語が 埋もれるが、診断の メモは タップで 開くので 増やしても 邪魔に ならない |
| ミーティング・リスニングの入室は**ドアのノックではなく Zoom の「さんかする 前の 画面」**にする。**カメラは既定 ON**（うつりたくない人はここで消してから入る）。マイクは押したときだけ**ためせる**（こえの大きさが棒で見える） | 2026-08-18 の指定。学習者がこれから出るのは会議室ではなく Zoom なので、練習する操作も Zoom の操作にそろえる。こわさは「相手に見られる前に自分で見る」順番でほどく |
| ミーティングの**しつもん・きょう やること・おわりの ひとこと**にも**ことばのポップアップ辞書**を出す。辞書の保存先は増やさず、**先生がスタジオ（DB）で直せる単語ステージ**をそのまま使う | 2026-08-18「ポップアップ辞書付き。DBで設定できるように。できれば既存のやつを使って」。読みもの（article-view）と同じ引き先（`src/lib/dictionary.ts`） |
| ~~ミーティングは**いつも1問目から**始める（途中の保存もしない）~~ → **画面を 更新しても だまって つづきから**。「つづきから 始めますか」の 確認は 出さない | 2026-08-21「画面更新などした場合でも途中からプレイできるようにしてほしい」。2026-08-18 の「いつも1問目から」を 取り消す。12問に なって 1回が 長く なった ため、途中で 落ちると やり直しが 重い。保存は `src/lib/meeting/resume.ts`（端末のみ・DBに 送らない）。話しきったら 消すので、次は はじめから 話せる |
| ミーティングは **2つの ばん**（ヘンディさんから しつもん／ヘンディさんに しつもん）。帯を 押して 行き来でき、**1つめを おえると 2つめの ロックが 外れる**。「はじまり」の ばんは 置かない | 2026-08-21 の指定。ばんの 区切りが 見えないと、どこまで やったのかが 分からない まま 流れる |
| ひとまとまり 話しきったら **しゅうりょうしょうの ポップアップ**を 出す（名前と 日付／話せた 数と ハート／今日 話せた ことの 一覧／つぎへ 進む ボタン）。**ラウンド1の あとと ラウンド2の あとの 2回** | 2026-08-21 の指定。会話の 練習は 終わった 瞬間に 消える ので、手に 残る ものに する（設計01 P13） |
| **相手の こえは 高さを 変えずに 速さだけ** 変える（ターンぶんを ためて WAV に し、`preservesPitch` で 鳴らす）。「ゆっくり 話して」と プロンプトで 頼まない | 2026-08-18「トーンはそのままでスピードだけ」／2026-08-21 実装。`playbackRate` を そのまま かけると 声が 別人に なる。Live 側に 速さの 設定は 無い（SDK の 型で 確認） |
| **つなぎが 切れたら 黙って つなぎ直す**（学習者に「マイクを つなぐ」を 出し直さない） | 2026-08-21「一定時間たつとマイクを繋ぐに戻る」。短命トークンは 30分で 切れる。張り直しの たびに 作り直すので 回数の 制限には 当たらない |
| こえは**ボタンを押している あいだだけ**送る（push-to-talk）。「話す」ボタンは**Zoomの画面の中**に、いちばん大きく置く | 2026-08-18「マイクの話すが小さい。押した時だけ話せるように。できればZoomUIの中に」。ずっと送っていると、まわりの音で相手のセリフが途中で止まる |
| ~~押している あいだだけ~~ → **1回押したら会話モード、もう1回押したら おわり**（トグル） | 2026-08-18 の再指定「押しながらではなく、1回押したら会話モード」。指を離すタイミングが分かりにくかった |
| ミーティングの進行は**アプリが持つ**。相手（Live）は受け止めるだけで、**自分から次の質問をしない**（「（しんこう）」の合図で聞く） | 2026-08-18 fable と相談（願い #80-5）。運転手が2人だと、画面の字幕と相手の話がずれて、学習者はどちらに答えるか分からない |
| 会話中の**ボタンは最小限**。「つぎへ →」「もう いちど 言う」「まだ 言えない」は置かず、進むのは判定にまかせる。詰まったときの出口は**ことば**（「すみません、つぎを おねがいします」） | 2026-08-18「ボタン操作少なめで会話中心がいいのでは？」。逃げ道すら日本語の産出にする |
| 会話の履歴は**Zoomのチャット欄のように**下へ伸ばし、スクロールで読み返せるようにする。伏せ札の「きょうの しつもん」はやめ、**「はなせた こと（n/m）」のチップ**にする | 2026-08-18「UIがすごくわかりにくい」「きょうの質問というのがわかりにくい」 |
| 相手の**話す速さ**を学習者が選べる（ゆっくり／ふつう／はやい）。**入る前と会話中の両方**で変えられる | 2026-08-18「相手の話すスピードを調整できるように。入室の時と話の時両方」。Live に速さの設定は無いので鳴らす側で変える |
| 学習者の足場は「こう 言えます」ではなく**「ヒント」**と呼ぶ | 2026-08-18「こういえますはかえってわかりにくい」 |
| マップから開く**せっていの画面**（`/map/settings`）は、**はじめの設定画面と同じUI**で、**性格診断はやらず保存だけ**できるようにする（なまえ・がっこう・せいべつ・Gemini APIキー） | 2026-08-18「最初の設定画面と同じUIでいいけど、性格診断なしで保存だけできるように」。欄は `src/components/learner-fields.tsx` を両画面で共有し、片方だけ直ってずれないようにする |
| もんだいの 出しかたは ~~学習者が えらぶ~~ → **`answerMode` の値の1つ（先生が決める）**。「1問ずつ」（`one`）／「1問ずつ見て最後に出す」（`submit`）／**「ぜんぶ 1ページに出す」（`all`）**。まとめて出すでは**「こたえを見る」を置かず**、採点は出したあとの1回だけ。書いたものは**端末に保存**する（Supabaseは使わない）。出したら**自分のこたえ・正解・全問の結果**を並べて見せ、**まちがえたもんだいだけ**やり直せる | 2026-08-19 の指定。「自分の選択肢と実際の結果がわからない」ため、**1問ずつでも えらんだものを画面に残す**（型を問わず「あなたの こたえ」を出す）。2026-08-23 に 出しかたの 持ち方を 決め直した：「全問1ページなら 出すのは 必然的に1回」なので **レベルを 別の軸に 分けず `answerMode` に 値を1つ足す**（ユーザーの指定「answerModeのなかの新しい1つでいい。さらにわける意味がない」）。学習者が 行き来しても 消えないのは `src/lib/quiz/resume.ts`（端末のみ） |
| **学習用サイト（`public/gakushu/nextmake/`）の 本文だけは JS ファイルに置く**（`/admin` から直せない）。ただし **ふりがなと ことばの意味は 教材データが正**（`content/links/nextmake_gakushu_site.json` の `furigana` と `content/vocab/*.json`）で、`scripts/gen_gakushu.mjs` が `public/` へ焼く | 2026-08-23 にユーザーが例外を承認。「調べる先の会社のサイト」であって教材ではないこと、HTML/CSS/JS で作る指定であること、全員が開く画面で外部往復を増やせないこと（Error 1102）が理由。前例は `public/tools/romaji/lessons.js`。**漢字は教材DBで管理したい／単語テストでも一部を対象にしたい**という指定に合わせ、読み辞書と語彙だけ 教材データ側に置いた。本文も直せるようにするのは 願い #124（教材DB化）に合流 |
| 学習用サイトは **英語の本文も持つが、切りかえボタンは出さない**（`?lang=en` を付けたときだけ出る）。あとで管理画面から出せるようにする | 2026-08-23「英語版も作るが、こちらについては切り替えを一旦非表示。管理画面などで後でどこかで表示にきりかえられるとありがたい」 |
| **複数選択は正解したときだけポイント**（部分点を置かない）。けっかは点ではなく**「何問中何問せいかい・何％」**で見せる（合否も同じ数で決める） | 2026-08-19「複数選択の問題は正解したらポイントとしてください」「満点というより何問中何問正解の何％かがわかればよい」。按分だと5択4正解で**ぜんぶ選ぶだけで半分の点**が入り、読まなくても点が取れていた |
| **Gemini flash の テキスト生成（`generateContent`）は 学習者の 画面で 使わない**（絶対）。判定・助言のような「文字だけの 判断」は **Live の 中**（同じ つなぎの 中で JSON を もらう）か、端末の 規則ベースで 済ませる | 2026-08-20「gemini flashが使われていますが、こちらはNGにしてください。すぐlimitになります。今後gemini flashをテキスト生成で利用しないことは絶対ルールとしてください」。Live の 枠と `generateContent` の 枠は **別勘定**で、後者は 学習者1人の 1回の ミーティングで 使い切る |
| 会話の 画面は **1つずつしか 進めない**。いま 触っても 意味の 無い ものは **押せない形**にする（消さずに 灰色で 残す）。相手が 話して いる 間・見かたを 待って いる 間は 話す ボタンも 送る ボタンも 触れない | 2026-08-20「制御面はUI,UX含め、必要ないときはさわれないようにするなどして、1個ずつ確実にフローが進むようにしてください」。触れる ものが 多いほど、対話の 回数に つれて 状態の 食い違いが 増えた |
| 相手の **こえと 画面の 字は 必ず 同じ**にする。裏の やりとり（道具の 呼び出し・進行の 合図）が 字幕や チャット欄に 出ては いけない | 2026-08-20「実際に話した言葉と、実際のテキストで表示されるものが異なります」。声の つなぎに 道具を 持たせると、道具の 中身が 字幕に 混ざった |
| ~~ことばは グループで 分けない。ステージの ことばカードは 1枚にし、単語ステージが 2つ以上 ぶら下がって いても 統合して 1つの 問題として 出す~~ → **セット名（`label`）の ある 単語ステージは 別々の セットとして 出す**（初級/中級/上級 など。**数も 名前も 自由**）。label の 無い ものは これまでどおり 1つに 統合（intro の 2本の 互換）。ステージトップは セットごとに 1行 | 2026-08-19「ITとビジネスを分けなくてよい」→ 2026-08-25「以前一ステージにつき単語は一つと言いましたが、複数必要になりました（初級者、上級者をわけるなど）」「上級初級はあくまで例。さらに複数あるかも」（願い #203）。実装は `src/lib/wordstage-merge.ts` |
| **教材は 全部 DB（`studio_contents`）で 管理する**。**JSON で データ管理しない**（次の先生が 書き換えられないので 困る）。無料枠に 問題が 無い かぎり すべて DB に 置く。DB の 構造を 変える 必要が あって それが 定まらない 間だけ、暫定で JSON に する ことは あり得るが、**そのときは 理由を 出して 別途 相談する**（AI が 勝手に JSON へ 寄せない） | 2026-08-19「全ての文章データはDBで管理されるべき。jsonだと次の先生が書き換えできないので困る」。2026-08-22 に 言い直し「JSONでデータ管理するな。無料版の制限などの問題がないようであれば全てDB上で管理できるようにしてほしい」——**AI が これを『git を 種として 残し git→DB へ 流し込む』と 読み違えて いた**（この行の 前の 版が それ）。無料枠は 問題に ならない（実測: DB 12MB／上限 500MB・ファイル 11MB／上限 1GB）。`studio_contents.data` は **jsonb** なので、教材の 形を 変えても DB の マイグレーションは 要らない。git の JSON が 要るのは 鍵ゼロの デモモード（CI の e2e）だけで、これは **正では なく 実行のための 写し**。設計は 願い #124 |
| ミーティングの **ヒントは ポップアップ**（出しっぱなしに しない）。話す ボタンと 会話の 記録を 押し出さない | 2026-08-20「ヒントはポップアップでお願いします」 |
| **採点は やさしく**。**意味が つたわれば 合格**、直しは アドバイスで 見せる。言い直しは **その場で 1回**まで（2回目の あとは かならず 進む）。合格と やり直しは **色・絵・ボタンの ことば**で はっきり 分ける | 2026-08-20「日本語の採点が厳しすぎます。その場で一度練習すればOKという形にして、意味が伝われば合格に」「合格とやり直しの見た目の違いがはっきりわかるように」 |
| マイクは **「スタート」を 押した ときに つなぎ、つながって から** マイクの UI を 出す（つながる 前に 押せる ものを 見せない） | 2026-08-20 の指定。つなぐ 前に 出して いた ため、**1回目の ひとことだけ 拾われなかった** |
| ミーティングの 画面は **添付の モックに 寄せる**（STEPの 道すじ／2つの タイル／6つの しつもんカード／しつもんの 吹き出し＋🔊／丸い マイク／右に チャット）。要らない ボタンは 置かない | 2026-08-20「UIですが、添付のものに極力近づけてください。不要なボタン（文字で入力する）などはなくてOK」 |
| **ふりがなの 色は 地の文に そろえる**。濃い色の ボタン・ラベル（白文字の 面）では ふりがなも 白にする。1か所ずつでは なく **`.btn-game rt` / `.text-white rt` を `currentColor` に**して 画面ぜんぶで そろえる | 2026-08-21「ボタンやラベルの色が濃いもののふりがなは白で表示させて」。既定の `rt` は 濃い灰なので、紺・桃・金の 面では ふりがなだけ 沈む。2026-08-18 の `RUBY_ON_COLOR` は 診断の 画面にしか 効いて いなかった |
| 性格診断の **導入は「チーム」から入る**（「性格とは何か」から入らない）。IT の しごとは チームで する → 人に よって 得意が ちがう → 自分と なかまの 得意を 知る、の順で 入り、得意の 例には **近い ネクマックスの 絵**を 添える | 2026-08-21 にユーザーが本文を指定。診断を受ける理由は「性格を知る」ことではなく「チームで働く」ことの側にある。呼び名は出さない（16の呼び名に会うのは結果画面が最初） |
| **生の こえは 待たずに 鳴らす**。届いた かけらを その場で 予約して 鳴らし、速さは **🔊 の 聞き返し**で 変える（`<audio>` ＋ `preservesPitch` なので 高さは 変わらない）。ターンぶん ためてから 鳴らす 作りに 戻さない | 2026-08-21「音声が 出てくるまでの ラグが 大きすぎる。wav を 待たずに、そのまま 音声出力させて、後で 聞き返す ための 🔈 ボタンを 押した時に、スピード調整の 内容に 合わせた 速さで」。ためる 作りは 高さを 守れる かわりに 数秒 黙る 相手に なった |
| **カードの 板は 見ている ばんに ついて くる**。帯（01/02）を 押したら、相手の 顔の すぐ下の 板も その ばんの ものに なる | 2026-08-21「02の 場合は 02の カードを 表示して」。帯だけ 02 で 板が 01 の ままだと、画面の いちばん 目立つ ところが 01 を 指しつづける |
| **おわる 道は 1つ**（Zoom と 同じ「退室」）。画面の 中に「◯◯を おわる」ボタンを 別に 置かない | 2026-08-21「『ミーティングを 終わる』ボタンは たいしつ が あるので 不要」 |
| **画面の ことばも 漢字＋ふりがな**。読みにくいからと ひらがなに 開かない（けす→消す・たいしつ→退室・ひらきます→開きます）。画面の 読みは 画面が 持つ（`ui-furigana.ts` など・教材の 読み辞書とは 混ぜない） | 2026-08-21「ひらきます・けす・たいしつ など、漢字を つかわず ひらがなで 書いて いる ものが あるが、かえって わかりにくい」。AGENTS.md 規律2 と 同じ 向き |
| **相手（Live）に 学習者へ しつもんを させない**。ラウンド2でも 緩めない。誘いは 疑問形で ない 固定句（「ほかにも 聞いて ください。」） | 2026-08-21 fable の 指摘。札の 当たり判定は 学習者の 発話だけを 見る ので、相手が 聞き返すと **その 答えで 札が 開く**（聞き出して いない のに 開く） |
| **どの 画面にも 前の 画面へ 戻る 道を 置く**（行き止まりを 作らない）。戻り先は **来た ところ**にそろえる——ステージから 入った 画面は ステージへ、一覧から 入った 画面は その 一覧へ。札の 字と 行き先を ずらさない | 2026-08-21「すべてのページにおいて、導線設計が不足しております。本来どのページに遷移すべきかを考慮して…全般的には『前の画面に戻る』導線が不足している箇所が多いです」。404 に 行き先が 1つも 無い・「まなびマップ」の 札が タイトル画面を 指す・ことばアーケードが いつも マップへ 出す、が 実在した |
| 松井社長との 対話は **ミーティングとは 別の UI**（対話ゲーム）。会話の 中身で **好感度が 上がり 100% を 目ざす**。サイトの「おもしろい ところ」を 5つ 見つけるまで AIが 深掘りし、そのあと **学生が 質問する ばん**に 変わる。返答は いくつかの 観点で 見る | 2026-08-23「松井社長は別なUIで作成してほしい」＋2026-08-24 の 指定（願い #177）。UI の 見本は 共感コミュニケーション（`sitsumon/practice`）。データは `meeting.talkGame`、点の 配り方は `src/lib/talkgame/affinity.ts` |
| **ユーザーが 用意した 問いは 全部 答えさせる**（AIの 判断で 落とさない）。多い・答えにくいと 思ったら、**問いを 減らさずに 手を 軽くする**——打つ問いを 語群えらびに 変える／全問に ページの ヒントを 付ける／合格の 線を 低いままに する | 2026-08-24「できれば用意した質問は答えてほしい」。26問の 調査シートを 22問に 絞ろうとして 止められた。分量の 心配は 答えかたの 側で 解く |
| 松井社長との 対話で 見つける「おもしろい ところ」は **3つ**（5つは 多い）。`talkGame.findCount` も 3 | 2026-08-24 の 指定。話す ばんが 長すぎると、いちばん 良い「学生が しつもんする ばん」に 届かない |
| 会社が できた 年は **年だけ 言えれば 合格**（月・日は 求めない） | 2026-08-24 の 指定。学習用サイトの あゆみには 月までしか 無く、「5日」は 仕事の 上で 意味を 持たない 知識（同 §採点はやさしく） |
| **会話教材の 点は「0点の 観点」を 残さない。** その ばんで 学習者が する ことに なって いる 観点は 必ず 1点 以上に する。逆に **その ばんで やって いない ことには 点を 置かない**（答える ばんに「しつもんの 形」を 置くと、内訳に「できて いない ✗」が 並び 責める 形に なる）。点は ターン数から 逆算する（1ターンの最大 = goal ÷ 上手な人のターン数／底 = goal ÷ 遅い人のターン数） | 2026-08-24「もう少し点数付けあってもよい？好感度ちゃんと上がるように設計できている？」。話す ばんの「ていねいさ」が 0点で、内訳に ✓ が 出るのに メーターが 動かなかった。実装は `src/lib/talkgame/affinity.ts` |
| **学習者が 見る 文は レベル（やさしい日本語／日本語）で 言葉が 変わる。** 答えを 1つに 決める 問いを 作るときは、**両方の 言い方を 正解に する**（技術は 道具／手段、リーダー／PM、新しい 技術／先端の技術） | 2026-08-24 に 26問を 作って 発覚。片方だけ 正解に すると、やさしい 日本語で 正しく 読んだ 学生が 落ちる |
| **学習者が 読む 文は N4学習中の レベルに そろえる。** 文法は N4 まで、1文は 30字（分かち書きの 空白こみ）まで。**ビジネス・IT の 語は 残す**——ひらがなに 開かず、漢字＋ふりがな＋辞書で 支える | 2026-08-25「日本語をN4学習中レベルに変換（ビジネスやITに必要な語は残す）」。ページ「会社の 調べかたを 学ぼう」は 30字超が 30件 あった |
| **教材の 絵は ぜんぶ イラストに する。** アイコン・ピクトグラムの 図は 使わない（余白の 多い 大きな アイコンは 説明の 理解を 助けない）。絵柄は **サイトの ほかの 絵**（まんがのコマ）に そろえる | 2026-08-25「アイコン画像ではなく、全てイラスト画像にすること」「画像が説明の内容の理解を促進しない（余白の多い大きなアイコン画像はつかわない）」 |
| **てじゅん（steps）は 1歩ごとに 絵を 置く**（絵は 小さく・正方形 80px の サムネイル）。**小項目にも 絵を 置く**。1枚の 絵に いくつも 描いて まとめない | 2026-08-25「ステップは5ステップなのに4つしか画像にない。ステップごとに画像を入れてほしい（ただし画像は少し小さくする）」「小項目に対しても画像を入れる」。`steps` ブロックの `images` で 実現 |
| **絵の 下に 文字（キャプション）を 出さない。** データの `caption` は `alt`（読み上げ）に だけ 使う | 2026-08-25「画像の下の文字は不要」 |
| **松井社長の 絵は 本人の 参考写真**（`public/img/characters/matsui/reference.jpg`）を 参照入力に して 描く | 2026-08-25「松井社長の絵を作る時は参考画像をつかって」。前の 絵は 白髪の 別人に なって いた |
| **もんだいの 設問に 見出し番号（A-3・D-13）を 付けない。** 設問カードに すでに 番号が 出て いる ので 2つの 番号は 混乱の もと | 2026-08-25「①などあるからいらない。すぐに理解できない勝手なルールを作ると混乱のもと」 |
| **「＊◯◯の ページ」の ヒントは 行を 変えて 出す**（データに 改行・表示は `whitespace-pre-line`） | 2026-08-25「＊実績の ページなどの前に改行入れられますか？」 |
| **調査シートは 全部 うめて 出して ほしい。** 出す まえの ひとことは「のこり Nもん。ぜんぶ 書いてから 出しましょう」。「おしても いいし、書いてからでも いいよ」の ような **何を すれば よいか 分からない 案内を 出さない** | 2026-08-25「全部埋めて出してほしいので、このメッセージは削除。特に前者は何が言いたいかわからないようなこういうコメントはいらないです」 |
| **ミーティングの「聞く ばん」は 教材ごとに 決める。** `discover`（開く 札）が 空の 教材は **相手の しつもんに 答えたら そこで おわり**——修了も その 時点で 書く。会社を知るの ヘンディさんは この かたち | 2026-08-25「会社を知るについては ヘンディへの 質問は 不要。次への ロックを 解除してほしい」。**ぜんぶ 答えたのに 先へ 進めない**状態に なって いた（修了を 聞く ばんの おわりでしか 書いて いなかった）。~~2026-08-21「ミーティングは 2つの ばん」~~ は 教材ごとの 選択に なった |
| **AIの 返事は 一覧に ある 漢字なら 使ってよい**（`src/lib/ai-kanji.ts`）。同じ 一覧から ルビの 索引を 作るので ふりがなが かならず 付く。一覧に 無い ことばは ひらがな、**国の 名前・外来語は カタカナ** | 2026-08-25「全部ひらがなで出力されて読みにくい。一部可能であれば簡単な漢字はつかってほしい。べとなむもひらがなだったので、それはカタカナにしてほしい」。~~「漢字は 1文字も つかわない」~~ を やめた |
| **もんだいの 板は「答えた ぶん＋いま 聞かれて いる 1つ」まで 押せる**。さきの しつもんへは 飛ばさない | 2026-08-25「今までに回答した問題＋次の問題までクリックOKにしてほしい」 |
| **AIが 「null」「なし」と いう 文字を 返す ことが ある。** 空として あつかい、その 欄ごと 出さない | 2026-08-25「アドバイスでデータがない時に ❗️null が表示されてます。項目ごと消すようにしてください」 |
| **同じ ことを 2回 聞かない。** 前の しつもんの 答えが そのまま 使える 聞き方は、聞き方を 変える | 2026-08-25「日本語とITが重複するので、質問の仕方を変えてください」。ヘンディさんの 6問目（何と 何を 学ぶ）と 7問目（3つの サポート）が どちらも「日本語と IT」で 始まって いた |
| **中身の 無い 入口を 出さない。** ステージに 単語ステージが ひもづいて いなければ、まなびマップの「単語を 勉強」も ステージのトップの「さいしょに ことばを おぼえる」も **出さない**（一覧へ 逃がす 代わりを 置かない） | 2026-08-25「ステージの単語がない場合は非表示にしてください」。札には「この ステージの ことば」と 書いて あるので、押した先が どの課か 分からない 一覧だと 学習者が 迷う |
| **まなびマップの「最初から」は ステージのトップを ひらく**（1本目の 教材を いきなり 開かない） | 2026-08-25「マップの『最初から』ボタンはステージのトップ画面を表示させてください（現在1個目のコンテンツをいきなり表示しています）」。何が 何本 あって どこまで 進んだかを 見てから 選べるように |
| **「辞書から えらぶ」は その ステージの 本文に 出て くる 語だけを 既定で 出す**（いま 出題中／本文に 出て くる の 2グループ）。全辞書から さがす 切替（ステージ絞り込み付き）は 残す | 2026-08-25「こちらで抽出された単語がステージの『辞書から選ぶ』で表示されるべき要素」「その中で選ばれたものが問題となります」（願い #203）。願い #128 の「辞書ぜんたいから 選べる」は 切替側に 残す |
| まなびマップの「単語を 勉強」札は **1枚のまま**、行き先を `/arcade/<ステージID>`（セット選択）に そろえる。**中間テスト対策の ような 横断セット**は、どの ステージにも ひもづけない 単語ステージとして 作る（/arcade 一覧に 出る） | 2026-08-25「マップは不可？削除？」「複数のステージの問題から一部の言葉を抽出する。どうすべき？」への 提案を 採用（願い #203） |
| **会社を知るの ことばは 3セット**: 初級=単語のみ／中級=単語＋センテンス／上級=センテンスのみ。**テストに 出す 語は 優先順で 絞る**（IT・ビジネスの 現場で 使う 語が 先。1セット 50語 目安）。**センテンス（「承知いたしました」の ような 定型文）も ことばの 正の 1語**として 持つ（term=文・reading=よみ） | 2026-08-25 の指定（願い #203）。~~232語 ぜんぶを テストに 出す~~ は 「初級中級上級でなんで6個? 3個の間違いでは？」で 取り消し。外れた 語は 辞書で 引ける |
| **ログインした時点で `profiles` に 登録する。** その とき **端末に しか 無い 情報**（ネクマックスの20問・なまえ・がっこう・せいべつ）も 一緒に 登録する。**DBが 正**で、端末の 情報で 埋めるのは 空いて いる 欄だけ | 2026-08-25「ログイン時にまだ登録されていないユーザーを登録するようにしてください。その時にネクマックスなどのローカルにある情報も登録してください」。DBの トリガー（`supabase/migrations/20260824090000`）は SQL を 手で 流して 初めて 効き、いちばん 最初の ログインでしか 動かず、**localStorage を 見られない**。実装は `src/lib/register-on-login.ts` |

| **ポップアップ辞書と 単語テストは 別の もの**。辞書（本文の ふきだし・`/dictionary`）は **ことばの正 ぜんぶ**から 引く（読む ための 助けなので 多くて よい）。単語テストの セットは **その中から えらんだ 一部**（おぼえる ものなので 少なく 厳しく）。**この 課題でしか 使わない 語は テストに 入れない**——よほど 学習の 効きが 大きい ものだけ。外しても 意味は 引ける | 2026-08-25「ポップアップ＝単語テストではない。ポップアップの中から単語テストに出る問題がある。実装に変更必要」「この課題以外で使う機会のない語はよほど学習でインパクトのある言葉以外覚えなくて良い」（願い #203）。前は 辞書を 単語テストを 畳んで 作って いたので、テストから 外すと 意味も 引けなく なって いた。引き先を 正に した（`src/lib/dictionary.ts`）|
| **ことばの ゲームは 合否と 正誤を 隠さない**: ⭕／❌ を **記号と ことばの 両方**で 出す（色だけに たよらない）。時間切れは **画面を 揺らして** 知らせる。けっかには **「合格」/「不合格」・点/満点・合格ライン・あと何点**を 出す。**途中で やめたら 合否は 出さない**（「とちゅうまで」）。**ゼロから やり直す 札は 置かない**（次の 一手は「まちがえた ことばだけ」） | 2026-08-26「単語ゲームで誤った語を入れても正解になってしまう」「正解と間違いの表示の違いがわかりにくい（色分けなどするか⭕️❌表示入れるなどして）」「時間切れになった時、画面が揺れるなどのエフェクトをつけて」「最終結果で『合格』『不合格』を明確に記載して…OKかNGがわからないのはストレス」「最終結果画面でゼロからやり直すボタンはなし」。**点の 数え方は 前から 正しかった**——外しても 当たっても 同じ 顔で 次へ 進んで いたのが 原因（`src/components/arcade/`） |
| **セットの ことばは 全問 出す**（`questionCount` = 語数）。まとめた セットは **足し算**で 全問 | 2026-08-26「まいにちつかうことば50個あるはずなのに20問しか出なかった。全問出してほしい」。1問は よみ＋いみの 2つなので、10語＝20回に 見えて いた。長く なる ぶん、**進み具合は 1問ごとに 保存**する（途中で 閉じても 消えない） |
| **先生が スタジオで 保存した ステージ（DB）は、git に 足した 教材を 隠す**。同一IDは DBが 勝つ ため。**git に 教材を 足したら、DBに その ステージの 行が あるかを 確かめる**（`/api/health/content` の `warnings`。ログイン不要） | 2026-08-26 の 事故: 会社を知るの ことばを 3セットに して 本番まで 出したのに、学習者には **初級しか 出て いなかった**。DBの `stage:kaisha`（2026-08-25 保存）が `wordStageIds:["stage23_kaisha"]` のままで、git の 3本を 隠して いた。git も DBも 壊れて おらず 検査は ぜんぶ 緑——**印が どこにも 出ない** 種類の 事故 |

| **ゲームの 最中の 文言は 最短**（記号中心）。⭕／❌／⏰ の しるしと ひとことだけで、**励ましの 2行を 出さない**。外した ときは **正しい こたえを いちばん 大きく** 出す。読みの 打ち直しは **❌ の しるしだけ**（文は 出さない） | 2026-08-27「ながい。もっとシンプルに正解不正解わかればいい。変にポジティブにするルールがあると思われるので、変えてほしい。ここにそれを適用しても無意味」「正解が何かをわかるようにそこだけフォーカス」。**規律1（「不正解」等の 禁止語）は そのまま**——変えたのは 長さと 置き場で、責める 言い方を 許した わけでは ない。遊んで いる 最中に 2行を 読ませると 手が 止まり、その間も ことばは 迫って くる |
| **迫る ことばに 下地（四角い 膜）を 敷かない**。読みやすさは 字の 黒ふちと 影で 作る。4択の 用語も **読みの ときと 同じ 顔**（白字＋黒ふち／ふりがなは 黄色＋黒ふち）。時計は 字の 後ろに 置かない | 2026-08-27「ふりがな問題の文字の周りに四角い膜があるのがカッコ悪い」「4択問題の迫り来る文字がよみにくい。読みの時と同じで良い」「4択は特にふりがなが読みにくい」。膜の 正体は **円い グラデーションを 四角い キャンバス全面に 塗って いた**こと（上下で 切れて 膜に 見える） |

## 運用の制約

| 制約 | 背景・出典 |
| ------------------------------------------------------------------ | ------------------------------------------ |
| **無料枠での運用は「Worker に仕事をさせない」が方針**。バックエンド処理を減らし、Supabase はできるだけブラウザから直に叩く（RLS が守る）。有料化（Workers $5/月）はユーザーの判断待ち | 2026-08-26「抜本的改革が必要です。supabase周りをフロントエンドにするとか、バックエンド処理を減らす方法」。20人同時利用で接続できない人が続出した日 |
| **デプロイ回数は 原因の ひとつ**（当初「無関係」と書いたのを 2026-08-26 夜に訂正）。1回のデプロイが KV へ約75件書き、枠は 1000件/日 ＝ **1日13回で使い切る**。使い切ったあとのデプロイは**作りおきゼロの版を公開する**ので、全アクセスがフルSSR → Error 1102。**授業のある日は、朝いちばんに1回だけ出す**（枠が戻るのは UTC 0時＝カンボジア朝7時・日本朝9時）| 2026-08-26「デプロイ数は無関係と思いますが…」。同日 16:03 の STG デプロイが実際に `code: 10048`（枠切れ）で作りおき 25/75 件しか入らなかった。冷えた Worker のフルSSR（docs/deploy.md §0.10）と合わせて二重の原因 |
| 検証環境を Vercel へ戻す案は**保留**（二重維持の手間に見合わない。上の枠切れは Cloudflare の KV 特有だが、$5/月 か デプロイ回数の抑制で解ける）| 2026-08-26 の同じ会話 |
| 確認依頼は「URL＋操作＋見るポイント」形式。散文で依頼しない | 2026-08-05 運用提案 v1.1 |
| 保存を伴う実機検証は検証専用アカウントで行う | 2026-08-04 本番データ上書き事故の再発防止 |
| ~~staging・本番の更新は main からのみ~~ → **STG の更新は `integration` から、本番の更新は `main` から**（2026-08-27）。ガードは**外さず、比べる相手を移した**（`scripts/preview_alias.mjs` は今も中身で判定する。ブランチ名の許可リストに足す方式は採らない） | 2026-08-03〜05 staging相互上書き事故の再発防止。2026-08-27「mainと同じブランチで動くSTGには価値がないと思います。各ブランチの変更が全てmainに統合しないとSTG確認できない状態のため、STG環境は別な統合ブランチにして、OKならmainに統合したい」 |
| **STG は `integration`（マージのたび自動）／本番は `main`（火・水・金 17:05 ICT に自動。授業は 17:30）**。緊急の直しだけ main へ直接 PR → 手動 dispatch → **main を integration へ戻しマージ**。**STG のデプロイは KV へ1件も書かない**（各ページ初回だけフルSSRで 1〜2秒。負荷の確認は本番に向けて行う） | 2026-08-27 の同じ会話。STG の更新が頻繁になるので、KV の書き込み枠（1000件/日）を STG が食い潰すと**その日の本番が作りおきゼロで出る**（2026-08-26 に実発生）。構造から断った |
| **ユーザーは PR・マージをしない（クライアント方式）**。AI が検証 → PR作成 → **`integration` へマージ** → STG 更新まで進め、ユーザーは **STG のURLで中身を見るだけ**。~~OK が出たら AI が本番へ上げる~~ → **本番は授業の前に自動で出る**（2026-08-21・2026-08-27） | 2026-08-11「私はクライアント。満足できるものをstgに。OKなら本番。私はPRをしない」 |
| AI の実機通し（ログインを含む検証）は Chrome（Claude for Chrome）で行う。ログインは実アカウントの選択のみ・認証情報の入力はしない | 2026-08-11「AIの通しはchromeでやってほしい」 |
| **ローカルでの実機検証をしない**（`npm run dev` を検証目的で立てない。マシン負荷のため）。検証はブランチ確認URL／STG で行う | 2026-08-11「ローカルサーバーの負荷がかかって困っている」 |
| **検証のサーバーはチェックが終わったら必ず閉じる**（立てっぱなしにしない）。Playwright も前に立てたサーバーを使い回さない | 2026-08-19「チェックが終わったら一度閉じるようにしてほしい。PCが重いことが増えた」。使い回しは**直す前のビルドに合格を出す**事故も起こした（同日実発生） |
| ユーザー確認を求める前に、**ユーザーが見られる形（ブランチURL/STG）まで進め、fable/Codex のチェックを先に通す**。ユーザーが確認できない中間物で承認待ちしない | 2026-07-28 に同文で2回矯正。「私の確認が必要になるまでは必ず進めるように」 |
| **デプロイ作業をユーザーにやらせない**（「この コマンドを 実行してください」と返さない）。鍵が無くて実行できないなら、鍵の要らない道（GitHub Actions の `デプロイ` ワークフロー）を先に作る | 2026-08-14「私にやらせないで。やらせる場合はそのルールに問題ある」。~~STG は main への統合で自動、本番は Actions から手動で出す~~ → **STG は `integration` への統合で自動、本番は授業前の時刻で自動**（2026-08-27）。**例外は「ワークフローを直した直後の承認」だけ**——AI の資格では承認 API が使えないので、人が GitHub の画面で 1回 Approve and run を押すしかない（docs/deploy.md §0.7） |
| **DBの 移行SQL も ユーザーに 貼らせない。** **`integration`** へ 入れば「デプロイ（DB）」ワークフローが 自動で 流す（docs/deploy.md §0.8。**DB が 常に コードより 先**になる 向き）。**DBに 依存する 直しを「直った」と 報告する 前に、DBに 入って いるかを 確かめる**（`npm run handoff` の「■ DB（移行SQL）」／鍵の 無い 場では Supabase コネクタで 直に 照会） | 2026-08-26「コネクタは前から繋がれていたので、今後もこのようなことがないように対策をお願いします」。`20260824090000` が 2日間 流されないまま、それに 依存する 直しを 2本 本番へ 出して いた。テストも e2e も CI も 全部 緑で、**DBだけが 遅れて いた**——30人が ログイン済みなのに 先生の 名簿は 23人。足りなかったのは 権限では なく **確かめる 仕組み** |
| AIのコミットは **`tomy1031` 名義＋`Co-Authored-By: Claude …` の付記**でそろえる（`git config user.email` は `33148568+tomy1031@users.noreply.github.com`）。名義がそろっていないと GitHub Actions の実行が「承認待ち（action_required）」で止まり、ユーザーに Approve を押させることになる | 2026-08-16「Aかな？承認はせず動くように設定」。他セッションはこの形で動いている |
| **ユーザーは動作確認をしない**。通しの確認は CI が自動でやる（アプリは Supabase 未設定なら**鍵ゼロのデモモードで起動する**ので、CI が毎回アプリを立てて6教材を通しプレイできる）。ユーザーが見るのは STG の**中身**だけ | 2026-08-16「これらの動作確認は私はしたくないので、今後しなくて済む方法を」 |
| Live対話の検証用 `GEMINI_API_KEY` は **GitHub の Environment Secrets（環境名 `Preview`）**に置く。使うのは **Playwright を走らせるステップだけ**で、**`.env` に書かない・ビルドのステップに渡さない**（OpenNext が `.env*` をバンドルへ焼き込むため。`scripts/check_build_env.mjs` が検査）。学習者の BYOK 方式は変えない | 2026-08-16 ユーザーが登録。鍵は localStorage 経由で学習者と同じ経路を通る（`src/lib/profile.ts`） |
| **Worker の大きさ（gzip）は無料枠 3MiB が上限**。2.8MiB を超えたら警告・3.0MiB で落とす見張りを CI に置く。原因は機能の量ではなく**ビルドの重複と圧縮漏れ**だった（2026-08-16 に 3066→1822 KiB へ削減） | 2026-08-16 デプロイが code:10027 で停止。「有料プランは選択肢なし・機能は減らさない」 |
| 実装はタスクの難易度に応じて **Opus / Sonnet を使い分ける**（複雑なエンジン実装＝Opus、機械的なデータ・文言作業＝Sonnet。設計判断・統合・検収は上位モデルが担う） | 2026-08-14「実装などはレベルに応じてopus sonnetを使い分けてください」 |
| **報告・依頼は「あなたがすること」を最初の1行に書く**。1文は短く。1行に情報を詰め込まない（リンク・番号・かっこの羅列をしない）。分析・経緯は後ろに回すか省く。テンプレを守っていても、行が濃ければ読めない | 2026-08-18「何してほしいのかさっぱりわからないことが多い。認知負荷高すぎる。今のまとめ方も意味不明。読めない」 |
| 分析を求められたら**表層（何が多い）で止めず、なぜ起きるか（どんなずれか）まで掘る**。「直しが多い」は分析ではない | 2026-08-18「直しのループが悪いという分析は無意味。なんで直しが発生するのかを分析して」 |
| 教材・会話UIは、**作った本人ではない目**（別セッション/エージェント）が、設計01 §4 失敗カタログ＋**意地悪条件**（文の途中で5秒止まる・声をつながない・言い直す・回線が切れる）で通しプレイしてから STG へ出す。作った本人の通しプレイは検収と数えない | 2026-08-19「OKやってみよう」（願い #97 承認）。ヒント消滅の穴（PR #92）はガイド記載済みなのに実装が踏み、後からの別目検収でしか見つからなかったため |
| ~~全ての編集は STG で確認してもらい、「本番OK」が出たものだけ本番へ~~ | 2026-08-18「全ての編集はSTGにあげてSTGで状況確認したい。OKなら本番に行ってほしいが、うまく行っていない」。同日 STG 14回更新に対し本番2回・3件が本番待ちのまま忘れられていた |
| ~~ヘンディさんは 出身の 国名を 言わない（「わたしも 外国から 来ました」で 通す）~~ → **出身地を きちんと 説明する**。国（インドネシア）・町（メダン）・来日の 年（2018年）を **固定の 事実**として 人格に 持たせ、聞かれたら 答える。札にも「ヘンディさんの 国の こと」を 置く | 2026-08-22「ちゃんと出身地も説明してほしい」。はじまりの まんがは 前から 「インドネシアから 日本に 来ました」と 言って いたのに、ミーティングの 人格だけが 伏せて いて、**同じ ステージの 中で 食い違って**いた（まんが側に そろえた）。`src/lib/content-checks.ts` の 合意ずみ地名にも 追加。**「タイ」の 禁止は 別の 運用判断なので そのまま** |
| **確認を待たずに、対応が終わったものから すぐ STG へ出す**。~~STG は通り道のまま（main へ入れば自動で更新）~~ → **STG が門番**（`integration` へ入れば自動で更新）。**AI が機械の検査（vitest・e2e・lint）を通したら `integration` へマージして閉じる。本番は火・水・金 17:05 ICT に自動で出る**ので、ユーザーの「本番OK」も AI の手動 dispatch も要らない（docs/deploy.md §0.6）。**これは「本番OK 待ち」の復活ではない——待つ相手は人ではなく時刻である。**「URL＋操作＋見るポイント」はこれまでどおり添える——見るのはユーザーの都合のよいときでよい | 2026-08-21「これチェックする時間がないため、対応終わったら速攻でデプロイしてください」。「本番OK まち」は、**待っているあいだ本番が古いままになる**ほうの害が大きくなった |
| ~~本番へ出すのは 1日に数回まで、まとめて~~ → **本番は 週3回（火・水・金）の 自動デプロイに まとめた**（2026-08-27）。STG は 何回 出しても よい（**KV書き込み 0件**にしたので 枠を 使わない） | 2026-08-22 に **Cloudflare の1日の書き込み上限で本番デプロイが落ちた**（`code: 10048`）。デプロイのたびに ISR キャッシュを KV へ載せるため、同日に STG+本番で12回出したところで枠が尽きた。枠は日付で戻る。**キャッシュを止めて回避しない**（ISR には KV が要る・Error 1102 の再発源） |
| **手もとで CI と 同じ 検査を ぜんぶ 回さない**（build・e2e全件・cf:build を 先に 回すと 約9分、そのあと CI が 同じ ことを 5分かけて もう一度 やる）。押す前は `node scripts/check_fast.mjs`（並列20秒。`git push` の pre-push が 自動で 回す）、e2e は **直した spec だけ**、大きさは CI の `size` に 任せる。**全件・大きさを 手もとで 回すのは、e2e 自体を 書き替えた ときと 順路・共有部品を さわった ときだけ** | 2026-08-25「完成までの速度が5倍以上になってしまっています。2時間平気でかかります。ちょっとした変更でも30分以上待つ状態です」。実測すると CI 1回は **約5分**で、小さな PR は 押してから STG まで **8分16秒**。長さでは なく **回数**が 効いていた——1本の ブランチが CI を **8回**回した 日が あり（3時間8分）、うち **3回が 赤**、1回は `format:check` が **9秒で** 落ちて いた（手もとで 20秒 払えば 出なかった 赤）。くわしくは docs/自動でたしかめる1枚.md §8 |
| **CI の 支度を キャッシュで 削る 余地は もう 無い**（願い #208・2026-08-26 実測）。`node_modules` だけ 効いた（1ジョブ 15〜23秒）。**Playwright の ブラウザ**は `--with-deps` の OS部品の 確認 16秒 が 残るので 5秒しか 減らず、**`.next/cache`** は ビルドが 静的ページの 生成 主体 なので 同じ SHA の 完全一致でも 変わらない。どちらも 外した | 見積もりでは「e2e 4分48秒 → 2分40秒（44%）」だったが、実測は **4分35秒 → 4分17秒**（ばらつき ±60秒 の 中）。**1回の CI を 短くするより 回す 回数を 減らすほうが 10倍 効く**（PR #207）を 裏づける 2例目。`e2e` 4分の 中身は ビルド 45〜64秒 ＋ 通し検証 121〜160秒 で、「やる ことを 減らす」以外に 手が 無い |
