---
name: browser-e2e-verification
description: ブラウザ自動操作でこのアプリ（本番・STG・ブランチURL）を検証するときに必ず発火。「実機検証して」「Chromeで通して」「E2Eで確認」「診断フローを通す」「自動でクリック/入力して確認」「画面を通しで確認」と言われたとき、および保存を伴う操作を自動化する前に使う。2026-08-04にユーザー本人の診断データを上書きした事故の再発防止。
---

本体は `docs/skills/browser_e2e_verification.md`。**作業前に全文を Read すること。**
ルールをここに写さない（二重管理禁止。本体は Codex/Gemini からも読まれるツール共通の台帳）。

最低限の鉄則だけ再掲: **保存を伴う操作は検証専用アカウントで。** 本人アカウントしか
使えないときは、先に SQL で現在値を退避してから始める。
