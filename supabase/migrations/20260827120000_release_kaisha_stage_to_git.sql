-- 「会社を 知る」ステージの DB行を 消して、git を もう一度 正に する
--
-- ## なぜ 要るか（2026-08-26 の 事故と 同じ型）
-- 合流の きまりは「同一IDなら DBが 勝つ」（設計07 §11.1）。`stage:kaisha` は
-- スタジオで 保存ずみ なので、**git の `content/stages/kaisha.json` を 直しても
-- 学習者の 画面は 1文字も 変わらない**。/api/health/content が
-- `stage:kaisha → source: "db"` と 出して いた とおりである。
--
-- 2026-08-27 に 配布資料（`会社研究/` の HTML 4枚）から この ステージを 作り直し、
--   * ページを 1本 足した（`kaisha_matsui_junbi`）
--   * もんだいを 1本 たたんだ（`kaisha_jibun` → `kaisha_omoshiroi` に 吸収）
--   * 並びを 6つの STEP から 5つに した
-- ——DBの 行が 残った ままだと、足した ページは **永久に 出ず**、たたんだ もんだいは
-- DBの 並びだけが 指したまま 残って **ステージの 途中で 404**に なる。
--
-- ## なぜ「消す」で 安全か（移行SQLの 決まり: いま動いているコードを壊さない）
-- 消しても、**アプリは 自分の ビルドに 焼き込んだ git の ステージへ 落ちる**
-- （`src/lib/content.ts` が git ∪ DB を 合流させ、DB側が 無ければ git を 使う）。
-- つまり:
--   * いま 本番で 動いて いる 古い コード … 古い ステージ（そのビルドの git）で そのまま
--   * 新しい コードが 出た あと     … 新しい ステージ（新しい ビルドの git）に なる
-- どちらの 順で 載っても 壊れない。**書き換え（update）では こうならない**——
-- 新しい 並びを 先に DBへ 入れると、古い コードが まだ 持って いない ページを
-- 指す ことに なり、2日ほど 404が 出る。
--
-- ## 先生の 直しは 消えるか
-- この 行に 入って いたのは、git と 同じ 中身の 写しである
-- （`/api/health/content` の `hiddenByDb` が 空＝ことばセットは git と 一致して いた）。
-- これ以降 先生が スタジオで 保存すれば、また DBの 行が でき、そちらが 勝つ。
-- そのときに git の 更新が 届かなく なる のは 同じ なので、
-- **`/api/health/content` の `warnings` を 見る**（教材の 並びも 見るように した）。

-- ## 2026-08-27: この SQL は **手で 流した**
-- 「デプロイ（DB）」ワークフローは `SUPABASE_DB_URL` が Environment「Preview」に
-- 無く、14秒で「鍵がそろっているか」に 落ちた（1回目・2回目 とも）。マージしただけでは
-- 学習者の 画面が 直らない ので、Supabase コネクタで この 1文を 流し、
-- `supabase_migrations.schema_migrations` に 版 20260827120000 を 記録した。
-- **鍵が 入れば、以後は ワークフローが この 記録を 見て 二重に 流さない。**

delete from public.studio_contents
where kind = 'stage'
  and id = 'kaisha';
