# Cloudflare Error 1102 恒久対応 — Claude 実装設計書

作成日: 2026-09-03

対象: NexmaxAcademy の学習者向け画面

実装担当: Claude

比較対象: `origin/main`（ローカル `main` ではない）

## 1. この作業の目的

授業中に学習者が同時アクセスしても、Cloudflare Workers の Error 1102 を出さない構造へ変更する。

達成したい状態は次のとおり。

1. 学習者が教材を開く通常操作では、Next.js のサーバー描画を実行しない。
2. 教材は静的な画面を先に表示し、必要なDBデータだけをブラウザからSupabaseへ問い合わせる。
3. 既存のURL、ログイン、成績、進捗、localStorageのキーを維持する。
4. 先生が既存教材を変更した場合、次に教材を開いた時点で新しい内容を取得できる。
5. 既存の応急処置を残したまま小さく移行し、問題があれば元へ戻せるようにする。

この作業はNext.jsの廃止ではない。Next.jsはルーティング、ビルド、静的画面、クライアントコンポーネントで引き続き使う。取り除くのは、学習者のアクセス中にWorker上で行っている重いサーバー処理である。

## 2. 障害の事実

### 2.1 発生時間

- 2026-09-02 17:30〜19:00（カンボジア時間）
- UTCでは2026-09-02 10:30〜12:00
- 授業中に本番環境でError 1102が頻発した。
- 同日、DBの値を変更した後にSTGが利用できない状態になったとの報告がある。

### 2.2 Error 1102の意味

授業中のログは次の状態だった。

```text
outcome: exceededCpu（CPU使用時間の上限超過で強制終了）
cpuTime: 10ms / 10ms（無料プランの上限）
Error: Worker exceeded CPU time limit
```

学習者ページのフルSSR（アクセス中にNext.jsがHTMLを作る処理）は、実測で次の時間を使っていた。

```text
280〜570ms / 10ms（上限）
上限の約28〜57倍
```

Cloudflareは一時的な上限超過を通すことがある。そのため `567ms / 10ms` でも成功する場合があるが、繰り返し超過すると `10ms` で強制終了される。成功した重いリクエストを「余裕がある証拠」にしてはいけない。

### 2.3 `/api/version` は負荷検査ではない

`/api/version` は、デプロイ中のコミットSHAとビルド日時を返すだけの確認用URLである。

```text
4ms / 10ms（上限）— この軽いURLは上限内
```

これは教材ページ、ログイン後の操作、Supabase取得が安全であることを示さない。

### 2.4 障害が繰り返された処理

```text
学習者が教材を開く
  ↓
ISR（古いページを裏で作り直す仕組み）が期限切れを検出
  ↓
OpenNextのmemoryQueueが自分自身へHEADリクエストを送る
  ↓
Next.jsが教材ページをフルSSR
  ↓
教材の読込・DB全公開行の取得・Git版との合流・画面生成
  ↓
280〜570ms / 10ms（上限）でError 1102
  ↓
作り直しが完了せず、ページの鮮度が更新されない
  ↓
次の学習者も同じ作り直しを行う
```

人数そのものよりも、「1人目で失敗した作り直しを全員が繰り返す構造」が問題である。

## 3. 現在入っている応急処置

`origin/main` では、学習者ページ約18ルートの `revalidate` を `300秒` から `604800秒（7日）` に変更している。

これにより、通常のデプロイ間隔ではページが古い判定にならず、授業中の作り直しが起こりにくくなった。STGのassetsモードでは `noopQueue（何もしない再生成キュー）` を使い、読み取り専用キャッシュへの再生成を止めている。

現在の本番とSTGは同じ修正後コミットを配信している。未ログイン状態の確認では、20並列×主要5URLの100リクエストが両環境とも全件応答した。

ただし、この確認でログイン必須URLはログイン画面への転送になっている。ログイン後の教材操作を100件確認したものではない。

### 応急処置の代償

- 先生がDBで変更した教材は、次のデプロイまで学習者画面へ反映されない。
- Worker上のフルSSR経路は残っている。
- キャッシュ設定の誤りや未生成ルートがあれば、再び重い経路へ入る可能性がある。

したがって、7日設定は恒久対応が完了するまで削除しないが、最終構造とはしない。

## 4. DB変更後にSTGが利用できなくなった件

現時点では「DB変更が直接CPU超過を起こした」と断定しない。次の2経路を分けて検証する。

### 4.1 STGに変更が出ない経路

STGの学習者ページは、デプロイ時の静的アセットに固定される。デプロイ後にDBを変更しても、次のデプロイまで学習者画面は変わらない。

### 4.2 DB行が教材から消える経路

`src/lib/content-db.ts` は `studio_contents.data` を `contentSchema.safeParse` で検査する。変更後の値がスキーマに合わない場合、その行は例外を表示せず空配列として除外される。

DB版がGit版より優先される構造のため、次を個別に確認すること。

- 変更した行の `id`、`kind`、`data.id`、`data.kind` が一致しているか。
- `status = published` か。
- `contentSchema` を通るか。
- ステージが参照する教材IDが存在するか。
- STGのデプロイ時刻とDB更新時刻の前後関係。

DBの不正な行を黙って除外するだけでは原因が分からないため、管理画面では保存・公開前に具体的な検査エラーを表示する。学習者画面では従来どおり安全な代替表示を使い、スキーマ詳細を見せない。

## 5. 採用する構造

### 5.1 基本方針

学習者ページを次の2層に分ける。

```text
静的な学習画面
  ├─ Next.jsがビルド時に生成
  ├─ Cloudflareの静的アセットから返す
  └─ Git由来の教材を初期表示として持てる

ブラウザの教材取得
  ├─ Supabaseの公開用anon keyを使う
  ├─ RLS（DB側のアクセス制限）を必ず通す
  ├─ 開いている教材IDだけ取得する
  └─ DBに公開版があれば静的版より優先する
```

### 5.2 Supabaseをブラウザから呼ぶ範囲

ブラウザから取得してよいもの:

- `studio_contents` の `status = published` の行
- ログイン中ユーザー自身のプロフィール、進捗、成績、保存データ
- RLSでそのユーザーに許可されたクラス情報

ブラウザへ出してはいけないもの:

- Supabase service role key
- 下書き教材を一般学習者が読める問い合わせ
- 他人の成績・進捗
- 管理者判定をクライアントだけで信頼する処理
- Geminiなどの共有秘密鍵

公開用anon keyは秘密鍵ではない。安全性はRLSで保証する。RLSの検査なしにクライアント移行を完了扱いにしない。

### 5.3 教材の取得単位

現在の `fetchDbContents()` は公開教材の全行を取得し、各種 `list*` 関数がそこから種類別に取り出す。React `cache()` で同一リクエスト内のDB往復は1回に畳まれているが、全行の受信・検査・合流・画面生成はWorker上に残る。

新しいブラウザ用リーダーでは、原則として次の条件をすべて指定する。

```text
id = 開いている教材ID
kind = 期待する教材種別
status = published
```

一覧画面でも必要な列だけを取得する。教材本文が不要な一覧で `data` 全体を取得しない。

目標:

- 詳細画面: Supabase問い合わせ1回
- 語彙など依存データがある画面: 最大2回
- 同じ画面で同じ問い合わせを重複させない
- 全公開教材の一括取得を学習者の通常操作からなくす

### 5.4 Git版とDB版の優先順位

既存仕様を維持する。

```text
同じ kind + id がある場合: DBのpublished版が優先
DBに無い、取得失敗、形式不正: ビルド済みのGit版を使用
```

ブラウザ側へ `GIT_CONTENTS` 全件を入れない。各ページに必要な静的教材だけをビルド時に渡すか、生成済みの小さな静的ファイルとして1件だけ取得する。

生成ファイルを使う場合、それはDBやGitに代わる編集元ではなく、ビルド成果物である。人が直接編集する運用を作らない。

### 5.5 教材変更の反映規則

既存教材の本文変更:

- 次にその教材を開いたとき、ブラウザがDB版を取得する。
- すでに操作中の教材は、途中で内容を差し替えない。
- React stateに最初に採用した `updated_at` を固定し、Realtime購読は行わない。

新しい教材ID・新しいURLの追加:

- 対応する静的ルートが必要なので、次のデプロイで利用可能にする。
- マップに新しいリンクだけ先に表示し、遷移先が未生成になる状態を作らない。
- 管理画面に「本文変更はすぐ反映、新規URLはデプロイ後」と表示する。

削除・非公開:

- 次に開いたときに利用不可と判定できること。
- 操作中の学生を突然追い出す必要はない。
- 新規遷移は止め、戻り先を表示する。

## 6. 既存ユーザーへの互換性条件

次を変更しない。

- Supabase AuthのユーザーIDとセッションクッキー
- プロフィール、進捗、成績、クエスト保存などの既存テーブル
- localStorageの既存キーと値の形式
- 学習者向けURL
- 教材コンポーネントが受け取るデータ型
- Git版とDB版の優先順位

DBマイグレーションが必要になった場合は、既存列の削除・改名を避け、後方互換の列追加にする。移行前後の両コードが同時に動いても壊れない順序にする。

デプロイ中に操作しているユーザーについては、次を確認する。

- 未保存入力がページ再読込で消えないか。
- 進捗保存直後に新しい版を開いても続きから再開できるか。
- 古いJavaScriptと新しい静的データが混在した場合に、再読込案内へ安全に落ちるか。

## 7. 実装フェーズ

### Phase 0: 実装前の基準値取得

変更前にログイン済み検証ユーザーを使い、次を記録する。

- `/map` を開く。
- ステージを開く。
- 教材詳細を開く。
- 問題を回答し、進捗を保存する。
- 戻る・再読込・再ログインを行う。

Cloudflareログでは、各操作の `outcome`、`cpuTime`、URL、例外を確認する。数値は必ず `実測値 / 10ms（上限）` で記録する。

保存ログAPIを利用するには、現在の認証に不足しているWorkers Observability権限を追加する。権限追加ができない場合は、`wrangler tail` を授業相当テスト中に起動し、ログをその場で保存する。

### Phase 1: 代表2画面の試作

対象:

1. `/map`
2. `[stage]/[content]` のうち、依存データが少ない教材種別1つ

実装内容:

- サーバー専用の `src/lib/content.ts` をクライアントからimportしない。
- ブラウザ専用の教材リーダーを追加する。
- 静的なGit版を即時表示できるようにする。
- DBの該当1件だけを取得し、成功後に採用する。
- 読込中、DB取得失敗、形式不正、非公開の表示を実装する。
- 操作開始後は教材を差し替えない。

この段階では他の学習者ルートを変更しない。

### Phase 2: 認証とWorker経路の計測

現在のmiddlewareは、認証クッキーがある場合に `getClaims()` を使って署名を確認する。外部認証サーバーへの毎回の往復は避けられているが、学習者リクエストでWorkerが起動する経路は残る。

代表2画面で次を比較する。

- middlewareを通る静的ページのCPU時間
- 未ログイン時とログイン時のCPU時間
- 静的アセットがWorkerを起動せず返せる構成

認証画面の表示制御をブラウザへ移す場合も、データ保護はRLSに任せる。画面を隠すことをセキュリティ境界にしない。

Cloudflareの静的アセット優先設定は、実装時点の公式ドキュメントとWrangler schemaを確認して決める。推測した設定名を書かない。`src/middleware.ts` を `proxy.ts` へ改名しない。

### Phase 3: 共通化

代表2画面の数値が合格した後に、次を共通化する。

- `kind + id` で公開教材1件を読む処理
- 静的版とDB版の合流
- 読込中・失敗・非公開の状態
- zodエラーの管理者向け記録
- 同一画面内の問い合わせ重複防止
- AbortControllerによる画面離脱時の中断

新しい大規模な状態管理ライブラリは、必要性が確認できるまで追加しない。

### Phase 4: 学習者ルートへの段階展開

一度に18ルートすべてを変更しない。依存関係が少ない順に移行する。

推奨順:

1. article / link / slides
2. manga / listening / skit
3. quiz / wordtest / dictionary
4. talk / meeting / quest
5. map / stage統合ルートの最終整理

各まとまりごとに、単体テスト、型検査、ビルド、ログイン済みE2E、同時アクセス確認を行う。合格してから次へ進む。

### Phase 5: ISR再生成経路の撤去

すべての学習者ルートが実行時SSRを必要としなくなった後にだけ行う。

- 学習者ページの `revalidate = 604800` を恒久仕様として残すか、完全静的へ変更するか決める。
- 学習者ページがmemoryQueueによる自己HEAD再生成へ入らないことを確認する。
- 本番用KVキャッシュが他のサーバールートで必要か監査する。
- 不要になった設定だけを削除する。
- 管理画面・APIの動的処理を誤って静的化しない。

応急処置を先に削除してから移行してはいけない。

## 8. 変更候補ファイル

実装前に `origin/main` の全文を読むこと。ローカルの古いブランチを正としない。

中心となる既存ファイル:

- `src/lib/content-db.ts`
- `src/lib/content.ts`
- `src/lib/supabase/client.ts`
- `src/middleware.ts`
- `open-next.config.ts`
- `wrangler.jsonc`
- `scripts/loadcheck.mjs`
- `supabase/migrations/20260803090000_studio_contents.sql`
- `src/app/[stage]/[content]/page.tsx`
- `src/app/map/page.tsx`

影響する学習者ルートは、`revalidate = 604800` を持つページを検索して確定する。

```bash
git grep -n "revalidate = 604800" origin/main -- 'src/app/**/page.tsx'
```

## 9. 必須テスト

### 9.1 単体テスト

- DB版が同じ `kind + id` のGit版より優先される。
- DBに行がなければ静的版を使う。
- DB取得失敗時に静的版を使う。
- `id` または `kind` が一致しないDB行を採用しない。
- スキーマ不正なDB行を採用しない。
- `draft` を学習者へ返さない。
- 操作開始後に取得結果が変わっても教材を差し替えない。

### 9.2 RLS検査

- 未ログインのanonクライアントがpublished教材だけ読める。
- 学習者がdraft教材を読めない。
- 学習者が他人の進捗・成績を読めない。
- 学習者が教材を更新・削除できない。
- 管理者だけが保存・公開できる。
- service role keyがブラウザbundleへ含まれない。

### 9.3 既存ユーザーE2E

- 既存ユーザーが再ログインなしで利用できる。
- 既存の進捗から再開できる。
- 回答・成績・クエスト保存が従来の行へ保存される。
- URLを直接開ける。
- 戻る、進む、再読込が動く。
- DBで既存教材を変更し、次に開いたときだけ変更が出る。
- 操作中の教材は途中で変わらない。

### 9.4 ビルド検査

最低限、次を実行する。

```bash
npm run format:check
npm run lint
npm run typecheck
npm run lint:content
npm run lint:secrets
npm test
npm run build
```

既存の失敗を消すためにテストを書き換えたり削除したりしない。作業前からの失敗は、今回の変更による失敗と分けて報告する。

### 9.5 授業相当テスト

未ログインの転送だけで合格にしない。ログイン済みセッションで、最低20人相当、可能ならプロジェクト制約に合わせて30人相当を確認する。

対象操作:

1. タイトルからログイン済み状態でマップを開く。
2. 同じステージを同時に開く。
3. 同じ教材を同時に開く。
4. 回答・保存を同時に行う。
5. 次の教材へ移動する。

## 10. 合格基準

### Cloudflare

- Error 1102: `0件`
- `outcome: exceededCpu`: `0件`
- 学習者ページのフルSSR: `0回`
- Workerを通る必要がある軽い処理: 目標 `p99 5ms以下 / 10ms（上限）`
- 単発最大値: 目標 `8ms未満 / 10ms（上限）`

### Supabase

- 詳細画面の問い合わせ: 原則 `1回`
- 依存データを含む画面: 最大 `2回`
- 学習者操作での全教材取得: `0回`
- RLS違反による他人・下書きデータ取得: `0件`

### ユーザー体験

- ログイン済み20並列の主要操作: `100%成功`
- 既存ユーザーのアカウント再作成: `不要`
- 既存進捗・成績の移行作業: `不要`
- DBの既存教材変更: 次回オープン時に反映
- 操作中の教材差し替え: `0回`

表示速度はカンボジアから実測し、変更前より悪化しないことを確認してから数値目標を固定する。手元の高速回線だけで決めない。

## 11. ロールバック

各段階で旧リーダーを残し、ルート単位で戻せるようにする。

ロールバック条件:

- Error 1102または500系エラーが1件でも再現する。
- RLS違反が見つかる。
- 既存の進捗・成績が読めない、または別行へ保存される。
- DB取得失敗時に教材を開けない。
- ログイン済み20並列テストが100%成功しない。

ロールバックしてもDBスキーマと既存データが壊れない設計にする。破壊的なマイグレーションを同じリリースへ含めない。

## 12. やってはいけない対応

- Next.jsを理由なく全面廃止する。
- 全教材データをブラウザbundleへ入れる。
- 学習者がページを開くたびに `studio_contents` 全行を取得する。
- service role keyをクライアントへ置く。
- RLSを無効にして動作確認する。
- 7日の応急処置を、恒久対応の検証前に戻す。
- `src/middleware.ts` を `proxy.ts` に改名する。
- `/api/health/content` や `/api/version` の成功だけで授業に出せると判定する。
- 未ログインの307転送だけを負荷試験にする。
- Cloudflareの一時的なCPU超過許容を前提にする。
- Workers有料プランへの変更だけで、重い経路を放置する。

有料プランは安全余裕を増やす選択肢だが、学習者アクセスごとのフルSSRや全教材取得を残す理由にはしない。

## 13. Claudeへの作業指示

1. 作業開始時にルートの `AGENTS.md` と該当設計資料を読む。
2. `package.json` に `handoff` がある場合だけ実行する。
3. `origin/main` をfetchし、実装ブランチは最新の `origin/main` から作る。
4. 現在の別ブランチ・未コミット変更を取り込まない。
5. まずPhase 0の基準値を記録し、Phase 1の代表2画面だけを実装する。
6. Cloudflare、Next.js、OpenNextの設定は、実装時点の公式文書とローカルのschemaを確認する。
7. 検査結果は `実測値 / 上限（リミット）` で報告する。
8. 何が確認できたかと、その検査だけでは何が分からないかを分けて書く。
9. 代表2画面が合格するまで、全ルート展開へ進まない。
10. 各フェーズを別コミットにし、ロールバック可能にする。

## 14. 完了条件

次のすべてを満たしたときだけ、恒久対応が完了したと報告する。

- 学習者ルートが実行時フルSSRへ入らないことをコードとCloudflareログの両方で確認した。
- ログイン済み20〜30人相当の授業操作が全件成功した。
- Error 1102と`exceededCpu`が0件だった。
- 既存アカウント、進捗、成績、URL、localStorageに互換性がある。
- published/draft、本人/他人、一般/管理者のRLS検査が通った。
- DB変更の反映規則が画面と運用文書に明記された。
- 失敗時に旧方式へ戻せることを確認した。
- 全テスト、型検査、ビルド結果を報告した。

「代表URLが開いた」「`/api/version` が4msだった」「未ログイン100件が転送された」だけでは完了にしない。
