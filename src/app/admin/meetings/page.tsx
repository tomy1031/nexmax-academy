import { redirect } from "next/navigation";

/**
 * 「ミーティングの きろく」は **「学習の きろく」に 畳んだ**（2026-09-04 の 指定
 * 「UXが低下しないなら学習の記録にたたみたい」）。
 *
 * ## 消さずに 送り返す
 * 先生の ブックマークと、これまでに 配った リンクが 404 に ならない ように する
 *（AGENTS.md「古いURLは消さず、本来のURLへリダイレクトする」）。
 * `?kind=talk` を 付けるので、開いた ときに **前と 同じ 中身の タブ**に 座る。
 *
 * ## 畳んで 何が 変わったか
 * 前の 画面に あった ものは ぜんぶ 移して ある——つまずきの まとめ（わるい順）・
 * まちがえた こたえ・AIの 見かたの 中身・生徒ごとの 何回目と 点・名前から その子の
 * 画面へ の リンク。そのうえで **所属（AUPP／CADT）・期生・メンバー・ステージ・
 * 単元の 絞り込みが 効き、CSV にも 出せる**（前の 画面には どちらも 無かった）。
 */
export default function Page() {
  redirect("/admin/records?kind=talk");
}
