/**
 * カードの ふち — 「どこまでが 要素か」を 見せる ための 枠
 *
 * これまでは **白フチ＋下だけの 硬い影**だった。白い地の 上に 白フチを 置くと 上と 横の
 * 境目が 消え、下に 出た 影だけが 浮いて 見える（2026-08-18 の指摘
 *「影の付け方が変。上下の縁や横の縁がないから どこまでが 要素か わからない」）。
 * 外側に 細い 青の 枠（`0 0 0 3px`）を 1本 足して、どの 辺を 見ても ふちが 分かるようにする。
 * 下の 硬い影は 島の 手ざわりなので 残す。
 *
 * **`globals.css` に 置かない**。あちらは 共有ファイルで 単独スレッドから 触れない決まり
 *（AGENTS.md 多スレッド運用ルール）なので、クラスの 文字列を ここで 配る。
 *
 * はじめの設定（welcome-wizard）と せっての画面（learner-fields）の 両方が これを 使う。
 * 片方だけ 直して ふちが ずれる、を 起こさない ため。
 */

/** 大きいカード（設問・導入・結果の 折りたたみ）。 */
export const CARD_EDGE =
  "border-4 border-white shadow-[0_0_0_3px_#a9d9f0,0_9px_0_3px_#c7e6f5,0_20px_30px_-16px_rgba(0,79,141,.4)]";

/** 小さいカード（なまえ・がっこう・せいべつ など）。枠と影だけ 浅くする。 */
export const CARD_EDGE_SM =
  "border-3 border-white shadow-[0_0_0_2px_#a9d9f0,0_6px_0_2px_#d7eaf5,0_14px_22px_-12px_rgba(0,79,141,.32)]";

/** 丸い帯・ふきだし（角丸が 大きいので 枠だけ 足す）。 */
export const CHIP_EDGE = "border-2 border-white shadow-[0_0_0_2px_#a9d9f0,0_5px_0_2px_#c7e6f5]";
